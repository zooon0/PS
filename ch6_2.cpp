#include <iostream>
#include <vector>

using namespace std;

void pick(int n, vector<int>& picked, int toPick);
int main()
{
    int n, picked, toPick;

}

void pick(int n, vector<int>& picked, int toPick)
{
    if (toPick == 0)
        return;
    else
    {
        pick()
    }
    
}